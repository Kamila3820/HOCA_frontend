import 'package:flutter/material.dart';

class PortraitLine extends StatelessWidget {
  const PortraitLine({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Colors.grey,
      thickness: 1.0,
    );
  }
}
